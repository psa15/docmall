<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.docmall.mapper.ReviewMapper">

	<!-- 리뷰 저장 -->
	<insert id="create">
	
		INSERT INTO
			TBL_REVIEW
				(R_NUM, M_USERID, P_NUM, R_CONTENT, R_SCORE)
		VALUES
			(SEQ_REVIEW_NUM.nextval, #{m_userid}, #{p_num}, #{r_content}, #{r_score})
	
	</insert>
	
	<!-- 리뷰 목록 + 페이징 -->
	<select id="getReviewList" resultType="com.docmall.domain.ReviewVO">
		<![CDATA[
		SELECT 
			RN, R_NUM, M_USERID, P_NUM, R_CONTENT, R_SCORE, R_REGDATE
		FROM(
		    SELECT 
		    	/*+ INDEX_DESC(TBL_REVIEW PK_REVIEW) */ 
		    	ROWNUM RN,  R_NUM, M_USERID, P_NUM, R_CONTENT, R_SCORE, R_REGDATE
		    FROM 
				TBL_REVIEW
		    WHERE
			 	P_NUM = #{p_num} AND ROWNUM <= #{cri.pageNum} * #{cri.amount}
		)
		WHERE RN > (#{cri.pageNum}-1) * #{cri.amount}	
		]]>	
	
	</select>
	
	<!-- 전체 리뷰 개수 -->
	<select id="totalReview" resultType="int">
	
		SELECT 
			COUNT(*)	
		FROM
			TBL_REVIEW
		WHERE
			P_NUM = #{p_num}
	
	</select>

</mapper>